# This project is Node/Playwright-based.
# Install these dependencies with npm ci (from package.json), not pip.
@playwright/test
@types/node
allure-commandline
allure-playwright
typescript

# npm ci
# npx playwright install